#ifndef MAIN_H
#define MAIN_H

#define DEFAULT_HEIGHT 350
#define DEFAULT_WIDTH 700

struct geometry {
  int	x;	/* The top left corner, horizontal coordinate */
  int	y;	/* The top left corner, vertical coordinate */
  int	h;	/* The height of the window */
  int	w;	/* The width of the window */
};

#endif /* MAIN_H */
